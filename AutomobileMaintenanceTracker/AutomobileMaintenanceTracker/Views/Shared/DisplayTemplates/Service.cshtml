

@model int?
@using AutomobileMaintenanceTracker.Helpers

@if (Model != null && Model > 0)
{
    @LookupValues.Service.Values.AsQueryable().Where(i => i.Value == Model.ToString()).First().Text
}
else
{
    @ResponseMessage.NoData
}